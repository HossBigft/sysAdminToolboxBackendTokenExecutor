package org.example;

public class ServiceCommand {

    public enum Plesk {
        GET_LOGIN_LINK,
        FETCH_SUBSCRIPTION_INFO
    }

    public enum Bind {
        REMOVE_DNS_ZONE
    }
}
